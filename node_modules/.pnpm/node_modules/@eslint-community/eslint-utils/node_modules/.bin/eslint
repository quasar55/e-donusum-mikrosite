#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../eslint/bin/eslint.js" "$@"
else
  exec node  "$basedir/../../../../eslint/bin/eslint.js" "$@"
fi
# cmd-shim-target=C:/Users/ertan/e-donusum-mikrosite/node_modules/.pnpm/@eslint-community+eslint-ut_44ab4bf9e27cfab471a4e977a69070ed/node_modules/eslint/bin/eslint.js
